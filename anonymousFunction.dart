void main() {
  print(sum(3, 5));
}

// anonymous function here ...
// Function sum = (int a, int b) => a + b;

// Argument types can be omitted in anonymous function. It’s a common thing.
Function sum = (a, b) => a + b;
